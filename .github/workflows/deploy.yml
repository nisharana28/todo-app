


   
name: Deploy Todo App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1 - Get the code
        uses: actions/checkout@v3

      - name: Step 2 - List files
        run: ls -la

      - name: Step 3 - Confirm deployment
        run: echo "Todo app successfully deployed to GitHub Pages!"